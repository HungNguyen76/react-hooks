import '../App.css'

function CSS() {
    return (
        <div style={{padding: '0 32px'}}>
            <h1 className="heading">CSS</h1>
        </div>
    )
}
export default CSS;

//development: npm start / yarn start -> CSS internal
//Producttion: npm run build / yarn build -> CSS external